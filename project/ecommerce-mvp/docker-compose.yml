# docker-compose.yml (PLACEHOLDER — no services yet)
# Notes for WSL users (no Docker Desktop):
# - Ensure Docker Engine runs inside WSL distro (e.g., Ubuntu).
# - Use Linux paths for volumes (e.g., /home/username/...).
# - Add services later: postgres, backend (elysia), frontend (nextjs).
version: "3.9"
services:
  # postgres:
  #   image: postgres:16
  #   environment:
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres
  #     - POSTGRES_DB=ecommerce
  #   volumes:
  #     - /home/youruser/.data/postgres:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  # backend:
  #   build: ./app/api
  #   depends_on: [postgres]
  #   environment:
  #     - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/ecommerce
  #   ports:
  #     - "3001:3001"
  # frontend:
  #   build: ./app/web
  #   depends_on: [backend]
  #   ports:
  #     - "3000:3000"